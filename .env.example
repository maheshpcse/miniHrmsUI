# Public frontend Pages-build example only.
# scripts/build-pages.js reads process.env; it does NOT automatically load this file.
# Set variables in your shell or GitHub Actions as documented in docs/DEPLOYMENT.md.
# Replace the example hostname. Never add database, SMTP or signing credentials.
API_URL=https://YOUR-SERVICE.up.railway.app/api
PAGES_BASE_PATH=/miniHrmsUI/
# Local npm start uses proxy.conf.js and /api; backend secrets belong in miniHrmsServer.
